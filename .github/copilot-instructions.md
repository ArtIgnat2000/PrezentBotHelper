# Инструкции для AI-ассистентов: Презентация Telegram Bot

## Обзор проекта
Это одностраничная HTML-презентация о проекте Telegram Bot для школьников. Полностью самодостаточный слайд-шоу с темами оформления, навигацией и адаптивным дизайном — всё в одном файле: `index.htm`.

## Архитектура
**Однофайловая структура**: весь HTML, CSS и JavaScript объединены в `index.htm` (744 строк). Нет процесса сборки, нет внешних зависимостей.

Ключевые секции:
- **Строки 1-357**: CSS стили с переменными тем и адаптивными точками останова
- **Строки 358-652**: 13 слайдов с семантическим HTML
- **Строки 653-744**: JavaScript для навигации и переключения тем

## Система стилизации

### Реализация тем
Две темы (тёмная/светлая) с использованием CSS переменных:
- Переключение тем сохраняет исходные цвета тёмной темы через атрибуты `data-*`
- Тёмная тема: `--color-bg-base: #0f172a`, `--color-primary: #38bdf8`
- Светлая тема: `--color-bg-base: #ffffff`, `--color-primary: #0284c7`

**Паттерн**: все зависящие от темы цвета ссылаются на CSS переменные, но встроенные стили их переопределяют. JavaScript вручную обновляет встроенные стили при переключении тем.

### Адаптивный дизайн
Три точки останова: 1024px, 768px, 480px
- Размеры шрифтов уменьшаются последовательно
- Макет переходит из строки в колонку на мобильных устройствах
- Отступы уменьшаются: 4rem → 2rem → 1rem → 0.5rem

**Утилит-классы** (вдохновлены Tailwind): `.row`, `.col`, `.center`, `.gap-lg`, `.mb-8`, `.text-2xl` и т.д.

## Функциональность JavaScript

### Навигация по слайдам
- **Состояние**: `currentSlideIndex` (0-12) управляет видимым слайдом
- **Сохранение**: `localStorage` сохраняет текущий слайд и предпочтение темы
- **Управление**: клавиатура (стрелки/Space) + кнопки навигации
- **Паттерн обновления**: все команды навигации вызывают `updateSlideView()`, которая переключает класс `.hidden`

### Переключение тем
Функция `toggleTheme()`:
1. Меняет классы `theme-dark`/`theme-light` на `<body>`
2. Вручную обновляет все встроенные стили (слайды, карточки, заголовки, блоки кода)
3. Восстанавливает исходные цвета тёмной темы из атрибутов `data-bg-dark`, `data-color-dark`
4. Сохраняет предпочтение в `localStorage`

**Важно**: инициализация данных темы происходит в IIFE на строках 654-669

## Структура контента

### Слайды (всего 13)
Каждый слайд: `<section class="slide col hidden" data-slide-id="X">`
- Титульный слайд: Слайд 1
- Слайды контента: 2-12 (возможности, стек технологий, примеры кода)
- Заключительный слайд: Слайд 13

**Паттерн**: все слайды имеют встроенные стили для `background-color` и `color`, которые должны обновляться при переключении темы.

### Счётчик слайдов
Каждый слайд заканчивается: `<div class="slide-counter">X/13</div>`
Позиция: `position: absolute; bottom: 1rem; right: 1.5rem;`

## Рабочие процессы разработки

### Тестирование
Откройте `index.htm` прямо в браузере — сервер не требуется.

Чек-лист тестирования:
- Навигация: стрелки, кнопки, состояние отключения
- Переключение тем: все цвета обновляются корректно, сохранение работает
- Адаптивность: тестируйте при 1024px, 768px, 480px
- Сохранение: обновите страницу, проверьте восстановление позиции и темы

### Внесение изменений

**Добавление нового слайда**:
1. Скопируйте существующую структуру `<section>`
2. Обновите `data-slide-id` и счётчик слайда
3. Обновите счётчик `totalSlides` в HTML навигации (строка 651)
4. Убедитесь, что встроенные стили соответствуют паттерну тем

**Изменение цветов**:
- Обновите CSS переменные в `:root` (строки 8-14) и классы тем
- Для встроенных стилей добавьте соответствующие атрибуты `data-*` для сохранения цветов тёмной темы
- Протестируйте обе темы после изменений

**Регулировка адаптивного поведения**:
- Измените медиа-запросы на строках 66-159
- Приоритет: мобильно-ориентированные паттерны, читаемость текста, размер сенсорных элементов

## Общие паттерны

### Компоненты карточек
```html
<div class="fill-width" style="background-color: #1e293b; color: #f1f5f9; padding: 2rem; box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);">
```
Паттерн: тёмные карточки на тёмных слайдах с тенью возвышения

### Блоки кода
```html
<pre style="background-color: #1e293b; color: #38bdf8; padding: 1.5rem; border-radius: 8px; font-size: 0.95rem; overflow-x: auto;">
```
Паттерн: подсветка синтаксиса через один цвет акцента, горизонтальная прокрутка при переполнении

### Использование эмодзи
Эмодзи для визуального акцента: 👨‍🎓 📚 🤖 ⚡ ✨
Паттерн: эмодзи + жирный заголовок + описательный текст

## Ключевые файлы
- `index.htm`: вся презентация (единственный файл для редактирования)
- `README.md`: минимальное описание проекта

## Заметки
- Язык: русский контент, английский код
- Нет внешних зависимостей (чистый HTML/CSS/JS)
- Скрипт Kaspersky на строке 7 (вероятно от антивируса, можно игнорировать)
- Имя папки проекта использует кириллицу: "Презентация ТГ бота"
